<UserControl x:Class="WIPR_Project.UCThongKeDoanhThu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:WIPR_Project"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             d:DesignHeight="330" d:DesignWidth="770">
    <Border Background="White" CornerRadius="15">
        <Wpf:CartesianChart x:Name="chartStar" Series="{Binding SeriesCollection}" LegendLocation="Left">
            <Wpf:CartesianChart.AxisX>
                <Wpf:Axis Title="Biểu đồ doanh thu 12 tháng" Labels="{Binding labels}" Foreground="Black" FontSize="16"/>
            </Wpf:CartesianChart.AxisX>
            <Wpf:CartesianChart.AxisY>
                <Wpf:Axis Title="Doanh thu" Labels="{Binding Ylabels}" LabelFormatter="{Binding Formatter}" Foreground="Black" FontSize="16"/>
                
            </Wpf:CartesianChart.AxisY>
        </Wpf:CartesianChart>
    </Border>
</UserControl>